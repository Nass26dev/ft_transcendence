from django.apps import AppConfig


class FriendsConfig(AppConfig):
    """Configuration de l'application friends (système d'amis)."""
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'friends'
